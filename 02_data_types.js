"use strict" // treat all JS code a s newer version
// alert(18 + 18) (we can't use alert in nodejs, we can use it in browser)

let name = "Manav"     // string
let age = 15           // number not int as c++
let isLoggedIn = true  // boolean
let state;             // undefined
let sum = null         // // null => standalone value

// null's datatype is object

// symbol => unique
// object

console.log(typeof name);           // string
console.log(typeof age);            // number
console.log(typeof isLoggedIn);     // boolean
console.log(typeof state);          // undefined
console.log(typeof sum);            // object

console.log(typeof "Raman");        // string
console.log(typeof 5);              // numbber
console.log(typeof false);          // boolean
console.log(typeof undefined)       // undefined
console.log(typeof null)            // object

